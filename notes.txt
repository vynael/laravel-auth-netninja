--- Docker Portainer
localhost:9000
username : admin
password : adminPortainerUbuntu

docker run --name ninja-network-auth \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=ninja-network-auth \
  -p 3307:3306 \
  -d mysql:8

docker run -d \
  -p 9000:9000 \
  --name portainer \
  --restart=always \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v portainer_data:/data \
  portainer/portainer-ce:latest


// untuk membuat folder menjadi repo git
git init

// add file
git add .
git add (nama_file)

// remote ke origin
git remote add origin https://github.com/vynael/laravel-auth-netninja.git

// git commit
git commit -m "first commit"

// git push
git push -u origin master

git clone .....

git pull origin master


after clone
composer install
npm install


-----------------
now tutorial #7
